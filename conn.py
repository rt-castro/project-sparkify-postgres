from creds import *

# connection string

connstring = ("host=%s dbname=%s user=%s password=%s" % (hostname, db, usr, passwd))

connstring_default = ("host=%s dbname=%s user=%s password=%s" % (hostname_default, db_default, usr_default, passwd_default))